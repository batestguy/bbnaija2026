# BBNaija 2026 Predictor — environment template
# The ONLY secret this project ever needs (P8 deploy: HF Space + Dataset sync).
# Copy to .env (gitignored) and fill in. Never commit .env.
HF_TOKEN=your_huggingface_write_token

# No other secrets exist in this architecture by design:
#   - no TWITTER_BEARER_TOKEN (X API removed from the project entirely)
#   - no GITHUB_PAT (no Colab push-back; deploys are push-triggered Actions)
