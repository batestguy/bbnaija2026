# knowledge.md — BBNaija 2026 Predictor

## What this project is
Zero-cost predictive analytics pipeline for Big Brother Naija 2026. **As of 2026-09-22 the engine is being replaced (owner decision):** the poll-matrix engine (long-format poll-observation matrix → weighted aggregation → softened official constraints → bootstrap, per `poll-matrix-engine-spec.md`) becomes the only model; the blog-CPI → PyMC MCMC → BMA engine is retired in place. Output stays **win probabilities** to the static dashboard (GitHub Pages primary, HF Static mirror).

## Current status — E-campaign (poll-matrix overhaul): E0–E4 + E6.1 + E5 DONE; E6.2 cutover = Sat Sep 26; P0–P8 MCMC build history below
- **2026-09-23 (latest, commits `2b88c2c` + `664f8fc`, deploys green):** E6.1 live rehearsal done. Two Saturday showstoppers found + fixed + test-locked: (1) result-article discoverer missed the real `week-N-vote-poll-result-and-eviction` slug; (2) `save_snapshot` duplicated `docs/polls.json` auto rows on re-runs — now upserts one auto row/week, human rows never touched. **Widget-timing model corrected and verified live:** during voting the widget renders the voting form with ZERO server-side percentages (results view is client-side only; every URL variant checked) → the Saturday in-window `state=empty` is BY DESIGN; the engine runs on transcribed seeds + carry-forward/momentum on Saturdays, and each week's finals land after close (post-close fetch or transcription) feeding the NEXT week's window. E5 backfill: result images archived for wk6/wk7/wk8; **wk1–5 result posts no longer exist on the site — permanently unrecoverable, gap-flagged**. `tests/test_model.py` DELETED (owner call: no MCMC runs → no MCMC tests; suite 123 passed in ~5s). Owner TODO before Sep 26: transcribe wk6+wk7 images (runbook §5) — takes the window from 1 to 3 seed weeks. Full detail: `docs/session-handoff-2026-09-23.md` (supersedes the 09-22 handoff).
- **2026-09-22, owner-approved overhaul:** poll-matrix engine replaces MCMC. Spec: `poll-matrix-engine-spec.md` (21 decisions, §12). Key deltas: share = P(win); 89% bootstrap CIs (overrides Readme §3.6's 95%); latest-wins dedupe; softened constraints on conflict; carry-forward for poll-uncovered actives; Gambit housemates excluded from the matrix (twist.json-driven; currently inert — Flora/Aikou released wk6); seed from transcribed result finals (wk-8 row already in `docs/polls.json`); ngnews → top-2 constraint rows; official-N adapter ships even if zero-rows; no midweek probes after cutover; cap=5000 stands. Certified debut **Sat Sep 26**; rollback = last-good `predictions.json`.
- E-phase detail lives in `PHASES.md` (E0–E6); build rules below in the MCMC sections apply to legacy files only (and legacy MCMC tests were removed 2026-09-23 with the owner's retire-entirely call).
- **GitHub repo: https://github.com/batestguy/bbnaija2026** (public, `main` tracked). Season 11 is LIVE (premiered 26 Jul 2026; finale ≈ 4 Oct). 
- **P5 VALIDATED** (`1c01585`): the R-hat NaN was ArviZ computing rhat over the LKJ diagonal (structural constant → 0/0 → NaN poisoned the max); fixed by finite-only rhat max. The divergences were a centered-parametrization funnel in the offsets block; **non-centered reparam + target_accept=0.95 → 0 divergences ×3 candidates, R-hat ≤ 1.006 at 4×2000**, on synthetic and real data. `s_beta` orphan fixed — the three BMA candidates now genuinely differ via the beta-column scale (momentum 2.0 / baseline 0.5 / heteroscedastic spike).
- **P6 DONE:** `run_weekly.py` exit-tested on real data (`38b80d3`) — scrape → preprocess (full-history rebuild, never week-sliced) → 3-candidate MCMC → BMA → schema gate → console review → auto-copy to `docs/`. Week-8 real posterior deployed: Temi Nkem 0.341 / Chimsom Chuka 0.171 / Tram 0.117, all gates green.
- **P7 DONE + P8 DONE:** dashboard live with real data (DEMO banner auto-clears); deploy workflow (push-trigger only, schema-gate-first) publishes **GitHub Pages (primary)** + **HF Static Space mirror** + **HF Dataset archive** (`85c3276`). Secret: `HF_TOKEN` (fine-grained, `bbnaija-deploy`). Photos: all 24 live.
- **Dashboard build-out (2026-09-17, `7e8a8e3`→`731c998`):** winner hero, freshness chip, OG/WhatsApp share cards, hover-tooltip chart (static annotations removed per owner), race-to-the-finale momentum strip (owner-approved replacement for the trend podium), red headline-vs-trend disagreement banner, Greek-letter rendering, bolder watermark, JJMB builders credit, tracker panel, mobile pass at 375px. README rewritten with the full model spec + screenshots. **Next session starts from `docs/session-handoff-2026-09-18.md`** (left-open: tracker auto-scoring ~20 min, P9 rehearsal, P11 closeout).
- **Week-8 DM remainder-bin bug FIXED (2026-09-18):** Yusuf's snapshot win prob was exactly 0.0 while every other active was slightly inflated — `dm_win_shares` passed `p[:-1]` to `rng.multinomial`, and numpy treats the **last PASSED pval as the remainder bin**, folding the last active column's mass into the second-to-last. Fix: pass the full probability vector (numpy consumes the final category internally; the old drift-shave hack existed only for the `p[:-1]` form and was removed). The frozen Gambit filter was innocent. Regression tests in `tests/test_model.py` (strong-column zero-guard + Gambit-zero preservation); model suite 16/16 green incl. the full synthetic end-to-end. The fix ships via the Sep 19 Saturday run — expect Yusuf small-but-nonzero and all medians to shed the stolen inflation; eyeball the BMA weights too (week-8 run came out suspiciously flat ~⅓/⅓/⅓).
- **Fan-poll tracking: TRACK-ONLY (owner decision 2026-09-18) — SUPERSEDED 2026-09-20:** unofficial polls (bbnaijadaily.com widget — up to 100 repeat votes/person; ngnews247 YouTube poll videos; FB voting groups) are logged by hand in `docs/polls.json` next to predictions. Week-8 scored: hazard concordance 38/55 pairs (0.691 vs 0.5 coin-flip); poll top-3 [Keivo, Temi Nkem, Ricky] vs model P(#1) top-3 [Temi Nkem, Chimsom Chuka, Flora]. Agreements: poll #1 (Keivo) == model's safest nominated (hazard 0.69); poll #11 (Chimsom Chuka) == model's most at-risk (1.82). The winner-model disagreement (quiet fanbases like Keivo/Ricky poll high, high-coverage housemates poll low) is the documented coverage-vs-vote-intensity limitation of CPI — known, stated, not patched.
- **Poll-anchored prior + multi-source poll scrape (owner decision 2026-09-20, supersedes track-only):** `src/scrape_polls.py` runs as a best-effort stage inside `run_weekly.py` (single-writer rule intact — no separate scheduled job). Sources, one isolated parser each: bbnaijadaily TotalPoll widget (server-rendered; full shares; state-aware — dark after Sat 21:00 close, never fabricated), ngnews247 YouTube RSS (rank-only title signal — concordance only, never shares), manual `docs/polls.json` rows (FB groups). **Anchor = weak alpha-column location shift** m_i = 0.25·clip(log(s_i·n), ±log 4), s_i = median normalized share across full-share sources, n = anchored count; scale/correlation/count formula untouched; **all-zero shift ≡ un-anchored spec** (gap-tolerant guarantee). Midweek snapshots only — **backfilled 'final' data never anchors** (outcome-leakage rule); past-week result articles are image-only (WhatsApp screenshots, verified wk-7) so backfill archives the image + flags needs-transcription for a human to transcribe into the manual log. Anchor recorded in `predictions.json` `priors.poll_anchor` + `polls` block, printed in the run review. Model/tests: 20 new fixture tests; full suite 91 green. The coverage-vs-vote-intensity limitation is now *anchored against* (weakly), not patched; P11 review remains the venue for a stronger design.
- **Remaining (post-overhaul):** owner transcribes wk6+wk7 result images before Sat Sep 26; E6.2 certified first publish Sat Sep 26 (expect `state=empty` in-window + seeds/momentum + THIN-WINDOW warning — that is the designed shape); post-close widget probe once before Oct 3; E6.3 run Sat Oct 3; finale ≈ Oct 4; P11 absorbs cap posture, anchor strength, Reddit/Telegram adapters, Gambit podium reconciliation, legacy MCMC removal. Saturday ritual doc: `docs/engine-runbook.md`.
- Product definition: **weekly predicted final standings** — each Saturday run publishes the podium projection (winner / runner-up / 2nd runner-up with slot probabilities) plus per-housemate P(#1)/P(top-3)/P(top-5), refreshed continuously as weekly data accrues.
- Files: `AGENTS.md`, `ENVIRONMENTS.md`, `PLAN.md` (approved), `PHASES.md` (lifecycle), `weekly-standings-spec.md` (approved deltas), `docs/season-recon.md`, this file.

## Key corrections to Readme.txt (do not regress)
- **No X/Twitter API anywhere.** Free read tier was removed (Feb 2026). Tweepy, `TWITTER_BEARER_TOKEN`, housemate handles, and Twitter-era rate-limit logic are **dead**. Data is blogs/RSS only: Google News RSS (primary) + BellaNaija/Pulse/DStv via BeautifulSoup4, **one isolated parser per source**.
- **Gambit twist is provisional** (no public record; S10-2025 winner was Imisi). Keep the eligibility-filter math byte-for-byte, but read `GambitFlag`/`Twist` from `config/twist.json` — never hardcode.
- **No cron.** Public-repo Actions minutes are free/unlimited (2,000-min quota is private repos only). Actions is **deploy-only, push-triggered**.
- **Colab is a documented alternative only** — no PAT push-back machinery, no `GITHUB_PAT`.

## Planned architecture (implement this split, nothing else)
`run_weekly.py` → `src/scrape_blogs.py` → `src/preprocess.py` (gap-tolerant; flags `missing_weeks`) → local `notebooks/bbnaija_mcmc` (PyMC + ArviZ) → `data/predictions.json` → push-triggered `.github/workflows/deploy.yml` → HF Space + Dataset repo + Pages mirror.

Specification file list: `run_weekly.py`, `config/twist.json`, `config/housemates.json` (canonical names, aliases, photo filenames), `src/scrape_blogs.py`, `src/preprocess.py`, `notebooks/bbnaija_mcmc`, `docs/index.html`, `docs/assets/script.js`, `docs/assets/photos/`, `.env.example` (HF token only).

Dashboard: trajectory line chart (top-5 medians + 89% CrI bands, photo+name end-labels, dropdown to add housemates) + below-chart panel (pairwise win-prob difference, P(A beats B), likely winner with HDI) + persistent Gambit warning banner + staleness badge + precision label + methodology note.

## Commands
- **No install/dev/test/lint/build exists yet** — no package files. Nothing to run until code is written.
- Planned runtime (Saturday, manual only, ~2 h window): `conda activate bap3` (or `causality-handbook`) → `python run_weekly.py [--lite]` (scrape → backfill-bridge → manual overrides → preprocess → MCMC → standings products → review → push).
- Environments (see `ENVIRONMENTS.md`): `bap3` (Py 3.11, pymc 5.8, arviz 0.16, numpy 1.24) or `causality-handbook` (Py 3.11, pymc 5.25, arviz 0.23, numpy 1.26, pandas 2.3.3). Use `-p <prefix>` for conda on the two prefix envs. Working copy + data live on **D:**; envs on **C:**. Sampling is CPU-bound — no GPU needed.

## Non-negotiable modeling rules
- **Gambit eligibility filter:** `WinProb_i = 0.0` if `GambitFlag_i == 1`, else `exp(μ_i) / Σ_{j ∉ Gambit} exp(μ_j)`. Zero out in **posterior predictive** — never just down-weight.
- **CPI (blog-adapted):** `0.4*Comments + 0.3*Shares + 0.2*ArticleMentions + 0.1*HeadlineFeatures`, min-max normalised per week before weighting; renormalise over available terms and log the renormalisation.
- **Count sub-model:** `log(μ_it) = α + α_i + (β + β_i)t + γSentiment + δAtRisk + θTwist + η(Twist_it·β_i)`; `α_i` shared as frailty in the Cox eviction sub-model.
- **Sampling:** 4 chains × 2000 draws (ZINB joint survival); 10k Dirichlet-Multinomial draws → median + 89% HDI. `--lite` fallback must label `precision` in `predictions.json` — never silently degrade.
- **BMA:** 3 candidates (momentum-heavy / baseline-heavy / post-Twist heteroscedastic `σ²_β` spike); Pseudo-BMA+ via ArviZ LOO ELPD with bootstrapping.
- **Priors are weakly-informative** (revised from the objective set): predictors standardized first; Normal(0, 2.5) fixed effects, HalfNormal(1) variance components, LKJ(2) correlations. No show-history priors. Prior predictive sanity + ~89% posterior predictive coverage are the alarm metrics.
- **Weekly standings machinery:** rank probabilities + podium from the 10k posterior draws (no new sampling); statistical ties on overlapping adjacent HDIs (alphabetical tie-break); secondary β_i trend-projected podium; Gambit housemates P(#1) ≡ 0 but runner-up/top-3/top-5 eligible.
- **Scope:** current season only — **no backtesting**. Convergence gate: reject any run with R-hat ≥ 1.01 (keep last good `predictions.json`, flag staleness).

## Constraints & gotchas
- **$0 ceiling:** no paid service anywhere. Only secret is an HF token for Space sync.
- **Manual-only Saturday runs** (user offline/machines off mid-week). No cron, no local schedulers, **single writer** — never add a scheduled scrape alongside manual runs (dual writers corrupt `data/`).
- **Calendar-true weeks:** `config/season.json` (premiere/finale dates) drives week numbering; missed Saturdays are backfill-bridged from archived scrapes (flagged, never fabricated); mid-week DQs/walkouts are coded as evictions via `data/raw/manual_notes.csv`.
- **Cross-blog voter independence is unverifiable** (same commenters recur across blogs; no $0 dedup key). Treat CPI as a correlated engagement index; state the limitation in README + dashboard methodology note; never claim independent voter sampling.
- Stack: BeautifulSoup4 + RSS, VADER (polarity ∈ [-1,1]), PyMC + ArviZ. `AtRisk`/`Twist` binary. Unmatched housemate aliases get quarantined for review, never silently dropped; missing photos fall back to initials avatars.
- Data schemas (`daily_cpi.json`, `predictions.json`) are pinned in `PLAN.md` §5 — match them exactly.
